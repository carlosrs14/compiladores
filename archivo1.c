#include <stdio.h>
int main(){
    int a, b, c = 5;
    float *dd = 0xFF;
    float gg = -23.34e4;
    a = 100;
    b = 1;
    while (a >= 0 || b == 2) {
        if(a)
            printf("%d",c);
        else 
            a++;
    }
    printf("Hola mundo");
    return 0;
}